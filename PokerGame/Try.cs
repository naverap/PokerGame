using Android.App;
using Android.Content;
using Android.Content.Res;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace PokerGame
{
    internal class Try
    {
        //    public class MainActivity : AppCompatActivity
        //    {
        //        ImageView img1;
        //        LinearLayout lean;
        //        protected override void OnCreate(Bundle savedInstanceState)
        //        {
        //            base.OnCreate(savedInstanceState);
        //            Xamarin.Essentials.Platform.Init(this, savedInstanceState);
        //            // Set our view from the "main" layout resource

        //            SetContentView(Resource.Layout.activity_main);
        //            lean = (LinearLayout)FindViewById(Resource.Id.li);
        //            LoadPics();




        //        }

        //        public void LoadPics()
        //        {
        //            Card card = new Card(Card.Suit.Clubs, Card.Value.Ace);
        //            int imageKey;
        //            this.img1 = new ImageView(this);
        //            LinearLayout.LayoutParams ivparams = new LinearLayout.LayoutParams(100, 100);
        //            ivparams.SetMargins(0, 0, 20, 0);
        //            this.img1.LayoutParameters = ivparams;
        //            this.lean.AddView(this.img1);
        //            string str = card.imageFile;
        //            Console.WriteLine(str);
        //            imageKey = Resources.GetIdentifier("" + card.imageFile, "drawable", this.PackageName);
        //            img1.SetImageResource(imageKey);
        //        }

        //    }
        //
    }
}